{
  "private": true,
  "scripts": {
    "build:web": "esbuild web/app.js --bundle --format=esm --minify --outfile=.web-build/app.js"
  },
  "devDependencies": {
    "esbuild": "^0.28.0",
    "svgo": "^3.3.2"
  }
}